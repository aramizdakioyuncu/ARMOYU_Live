name: armoyu_desktop
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+2

environment:
  sdk: ">=3.4.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  get: ^4.6.6
  armoyu_services: ^1.0.0+57
  armoyu_widgets: ^1.0.0+78

  socket_io_client: ^3.0.0
  flutter_webrtc: ^0.14.0
  cached_network_image: ^3.4.1
  cupertino_icons: ^1.0.6
  permission_handler: ^12.0.0+1

  audioplayers: ^6.1.0
  record: ^6.0.0
  window_manager: ^0.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/sounds/
